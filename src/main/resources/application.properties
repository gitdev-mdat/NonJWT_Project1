# Server Configuration
server.port=8081

# Spring Application Configuration
spring.application.name=estatebasic

# Disable R2DBC Auto Configuration (we use JDBC instead)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.r2dbc.R2dbcAutoConfiguration

# MySQL Database Configuration
# (Currently using hardcoded values in Repository, but can be configured here)
# spring.datasource.url=jdbc:mysql://localhost:3306/estatebasic
# spring.datasource.username=root
# spring.datasource.password=123456
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
